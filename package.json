{
  "name": "apecerto-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs && node scripts/rotas.mjs",
    "extract-design": "node scripts/extract-design.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
